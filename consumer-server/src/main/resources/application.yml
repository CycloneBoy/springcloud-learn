server:
  port: 9010
spring:
  application:
    name: consumer-server  #全部小写

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
#      defaultZone: http://peer1:8000/eureka/
  instance:
    prefer-ip-address: true
    
