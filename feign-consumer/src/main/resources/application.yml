server:
  port: 9040
spring:
  application:
    name: feign-consumer  #全部小写

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
#      defaultZone: http://peer1:8000/eureka/
  instance:
    prefer-ip-address: true
    
